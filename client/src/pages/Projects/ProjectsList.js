import React from 'react';
import PublicProjects from './PublicProjects';

const ProjectsList = () => {
  // Use the real public projects page that fetches from API
  return <PublicProjects />;
};

export default ProjectsList;
